apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: admin-webapps
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  destination:
    namespace: webapp
    server: https://kubernetes.default.svc
  sources:
    - repoURL: https://github.com/NHristovski/masters-degree-thesis-argocd-app.git
      path: platform-charts/username-webapps
      targetRevision: main
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
