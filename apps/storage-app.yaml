apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ceph-block-storage
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  destination:
    namespace: ceph-block-storage
    server: https://kubernetes.default.svc
  sources:
    - repoURL: https://github.com/NHristovski/masters-degree-thesis-argocd-app.git
      path: platform-charts/ceph-block-storage
      targetRevision: main
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
