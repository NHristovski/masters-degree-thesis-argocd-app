apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: a
spec:
  parentRefs:
    - name: testing-cilium-gateway
      sectionName: https
      namespace: cilium
  hostnames:
    - "www.192.168.100.240.sslip.io"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: a
          port: 80
