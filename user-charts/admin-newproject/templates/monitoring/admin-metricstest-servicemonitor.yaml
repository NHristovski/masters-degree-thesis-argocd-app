apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: metricstest
spec:
  selector:
    matchLabels:
      app: metricstest
  endpoints:
    - targetPort: 8080
      path: /actuator/prometheus
      interval: 30s
