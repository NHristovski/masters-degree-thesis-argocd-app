apiVersion: v1
kind: Service
metadata:
  name: metricstest
spec:
  type: ClusterIP
  selector:
    app: metricstest
  ports:
    - name: http
      port: 8081
      targetPort: 8081
      protocol: TCP
