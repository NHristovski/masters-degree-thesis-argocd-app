apiVersion: v1
kind: Service
metadata:
  name: admin-metrics
spec:
  type: ClusterIP
  selector:
    app: admin-metrics
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
