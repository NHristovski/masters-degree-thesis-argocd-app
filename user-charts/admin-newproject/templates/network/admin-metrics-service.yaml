apiVersion: v1
kind: Service
metadata:
  name: admin-metrics
spec:
  type: ClusterIP
  selector:
    app: admin-metrics
  ports:
    - name: http
      port: 30000
      targetPort: 30000
      protocol: TCP
