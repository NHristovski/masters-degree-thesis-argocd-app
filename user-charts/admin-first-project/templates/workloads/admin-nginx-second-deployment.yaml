apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-second
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nginx-second
  template:
    metadata:
      labels:
        app: nginx-second
    spec:
      containers:
        - name: nginx
          image: docker.io/nginx:1.27-alpine
          ports:
            - containerPort: 80
