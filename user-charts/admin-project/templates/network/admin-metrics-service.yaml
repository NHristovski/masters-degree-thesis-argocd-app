apiVersion: v1
kind: Service
metadata:
  name: metrics
  labels:
    app: metrics
spec:
  type: ClusterIP
  selector:
    app: metrics
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
