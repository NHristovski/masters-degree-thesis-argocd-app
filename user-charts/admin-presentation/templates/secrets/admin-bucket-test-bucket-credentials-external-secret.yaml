apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: bucket-test-bucket-credentials
spec:
  refreshInterval: "7500s"
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: bucket-test-bucket-credentials
    creationPolicy: Owner
  data:
    - secretKey: ACCESS_KEY_SECRET
      remoteRef:
        key: kv/bucket-test-bucket-credentials
        property: ACCESS_KEY_SECRET
