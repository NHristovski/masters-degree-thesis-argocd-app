apiVersion: apps/v1
kind: Deployment
metadata:
  name: testfunc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: testfunc
  template:
    metadata:
      labels:
        app: testfunc
    spec:
      containers:
        - name: nginx
          image: docker.io/nginx:1.27-alpine
          ports:
            - containerPort: 80
