apiVersion: apps/v1
kind: Deployment
metadata:
  name: tt
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tt
  template:
    metadata:
      labels:
        app: tt
    spec:
      containers:
        - name: nginx
          image: docker.io/nginx:1.27-alpine
          ports:
            - containerPort: 80
