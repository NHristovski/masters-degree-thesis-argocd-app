apiVersion: apps/v1
kind: Deployment
metadata:
  name: tst
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tst
  template:
    metadata:
      labels:
        app: tst
    spec:
      containers:
        - name: nginx
          image: docker.io/nginx:1.27-alpine
          ports:
            - containerPort: 80
