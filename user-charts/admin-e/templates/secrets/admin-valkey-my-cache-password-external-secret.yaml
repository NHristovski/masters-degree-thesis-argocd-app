apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: valkey-my-cache-password
spec:
  refreshInterval: "7500s"
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: valkey-my-cache-password
    creationPolicy: Owner
  data:
    - secretKey: password
      remoteRef:
        key: kv/valkey-my-cache-password
        property: password
