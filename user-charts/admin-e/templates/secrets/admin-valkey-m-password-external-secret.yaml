apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: valkey-m-password
spec:
  refreshInterval: "7500s"
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: m
    creationPolicy: Owner
  data:
    - secretKey: password
      remoteRef:
        key: kv/valkey-m-password
        property: password
