apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: valkey-password
spec:
  refreshInterval: "7500s"
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: valkey-password
    creationPolicy: Owner
  data:
    - secretKey: password
      remoteRef:
        key: kv/valkey-password
        property: password
