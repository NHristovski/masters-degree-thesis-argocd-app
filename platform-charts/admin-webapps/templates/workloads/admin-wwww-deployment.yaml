apiVersion: apps/v1
kind: Deployment
metadata:
  name: wwww
spec:
  replicas: 1
  selector:
    matchLabels:
      app: wwww
  template:
    metadata:
      labels:
        app: wwww
    spec:
      containers:
        - name: nginx
          image: docker.io/nginx:1.27-alpine
          ports:
            - containerPort: 80
