apiVersion: apps/v1
kind: Deployment
metadata:
  name: w
spec:
  replicas: 1
  selector:
    matchLabels:
      app: w
  template:
    metadata:
      labels:
        app: w
    spec:
      containers:
        - name: nginx
          image: docker.io/nginx:1.27-alpine
          ports:
            - containerPort: 80
