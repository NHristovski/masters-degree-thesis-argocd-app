apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: credentials
spec:
  refreshInterval: "7500s"
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: credentials
    creationPolicy: Owner
  data:
    - secretKey: password
      remoteRef:
        key: kv/credentials
        property: password
