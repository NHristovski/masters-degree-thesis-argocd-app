apiVersion: apps/v1
kind: Deployment
metadata:
  name: webapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: webapp
  template:
    metadata:
      labels:
        app: webapp
    spec:
      containers:
        - name: nginx
          image: docker.io/nginx:1.27-alpine
          volumeMounts:
            - name: s1
              mountPath: /data
          ports:
            - containerPort: 80
      volumes:
        - name: s1
          persistentVolumeClaim:
            claimName: username-s1
