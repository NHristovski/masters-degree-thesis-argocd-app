apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: fun
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: b-cilium-gateway
      namespace: cilium
      sectionName: https
  hostnames:
    - "fun.192.168.100.244.sslip.io"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - group: ''
          kind: Service
          name: retry-proxy
          port: 80
          weight: 1
