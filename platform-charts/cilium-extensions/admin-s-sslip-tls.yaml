apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: s-sslip-tls
  namespace: cilium
spec:
  secretName: s-sslip-tls
  dnsNames:
    - "*.192.168.100.242.sslip.io"
    - "192.168.100.242.sslip.io"
  issuerRef:
    name: vault-cluster-issuer
    kind: ClusterIssuer
