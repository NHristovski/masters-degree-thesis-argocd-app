apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: e-sslip-tls
  namespace: cilium
spec:
  secretName: e-sslip-tls
  dnsNames:
    - "*.192.168.100.250.sslip.io"
    - "192.168.100.250.sslip.io"
  issuerRef:
    name: vault-cluster-issuer
    kind: ClusterIssuer
