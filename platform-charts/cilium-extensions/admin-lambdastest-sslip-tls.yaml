apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: lambdastest-sslip-tls
  namespace: cilium
spec:
  secretName: lambdastest-sslip-tls
  dnsNames:
    - "*.192.168.100.253.sslip.io"
    - "192.168.100.253.sslip.io"
  issuerRef:
    name: vault-cluster-issuer
    kind: ClusterIssuer
