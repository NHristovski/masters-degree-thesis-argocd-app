apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: d-sslip-tls
  namespace: cilium
spec:
  secretName: d-sslip-tls
  dnsNames:
    - "*.192.168.100.252.sslip.io"
    - "192.168.100.252.sslip.io"
  issuerRef:
    name: vault-cluster-issuer
    kind: ClusterIssuer
