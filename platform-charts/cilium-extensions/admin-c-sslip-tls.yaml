apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: c-sslip-tls
  namespace: cilium
spec:
  secretName: c-sslip-tls
  dnsNames:
    - "*.192.168.100.253.sslip.io"
    - "192.168.100.253.sslip.io"
  issuerRef:
    name: vault-cluster-issuer
    kind: ClusterIssuer
