apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: username-test
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ceph-block-storage
