apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-postgres-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-block-storage
  resources:
    requests:
      storage: 3Gi

