apiVersion: v1
kind: Service
metadata:
  name: nodeport-grafana
spec:
  type: NodePort
  selector:
    app.kubernetes.io/instance: monitoring
    app.kubernetes.io/name: grafana
  ports:
    - name: nodeport
      port: 80
      protocol: TCP
      targetPort: grafana
      nodePort: 30090
