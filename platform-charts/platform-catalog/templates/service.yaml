apiVersion: v1
kind: Service
metadata:
  name: platform-catalog
spec:
  type: NodePort
  selector:
    app: platform-catalog
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30088
      protocol: TCP

