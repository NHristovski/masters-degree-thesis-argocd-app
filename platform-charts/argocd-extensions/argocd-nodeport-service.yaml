apiVersion: v1
kind: Service
metadata:
  name: nodeport-argocd
spec:
  type: NodePort
  selector:
    app.kubernetes.io/component: server
    app.kubernetes.io/name: argocd-server
    app.kubernetes.io/part-of: argocd
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30091
