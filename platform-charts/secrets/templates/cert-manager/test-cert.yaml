apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: sb-tls
spec:
  secretName: sb-tls-secret
  dnsNames:
    - sb.192.168.100.241.sslip.io
  issuerRef:
    name: vault-cluster-issuer
    kind: ClusterIssuer